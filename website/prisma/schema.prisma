// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String
  role      String   @default("USER") // ADMIN, HR, USER
  
  // 👇 เพิ่มข้อมูลส่วนตัว (ใส่ ? แปลว่า "มีหรือไม่มีก็ได้" จะได้ไม่ error กับ user เก่า)
  fullName  String?  // ชื่อ-นามสกุลจริง
  email     String?  // อีเมลติดต่อ
  phone     String?  // เบอร์โทร
  position  String?  // ตำแหน่งงานปัจจุบัน (เช่น Senior Developer)
  bio       String?  // แนะนำตัวสั้นๆ
  
  createdAt DateTime @default(now())
  jobs      Job[]
}


// ตารางใหม่ที่เราต้องเพิ่ม
model Job {
  id          Int      @id @default(autoincrement())
  title       String   // ชื่อตำแหน่ง (เช่น Frontend Developer)
  department  String   // แผนก (เช่น IT)
  location    String   // สถานที่ทำงาน
  salary      String?  // เงินเดือน (ใส่ ? แปลว่าไม่ระบุก็ได้)
  description String   // รายละเอียด
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // เชื่อมกับคนโพสต์
  author      User     @relation(fields: [authorId], references: [id])
  authorId    Int
}